APP_ENV=development
HOST=0.0.0.0
PORT=3000
DATABASE_URL=postgresql://devdbszusr:change-me@postgres-dev:5432/singziller_dev

# Optional runtime security settings.
# Fill these in local/server-only .env files, never in committed files.
CORS_ORIGINS=http://localhost:4173
SESSION_SECRET=change-me-minimum-32-characters
JWT_SECRET=change-me-minimum-32-characters
